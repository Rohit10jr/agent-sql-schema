Hi {{ user.first_name|default:"there" }},

We received a request to reset your {{ site_name }} password. Visit this link to set a new one:

{{ reset_url }}

This link expires in {{ expiry_hours }} hour{{ expiry_hours|pluralize }}.

If you didn't request this, ignore this email — your password won't be changed.
